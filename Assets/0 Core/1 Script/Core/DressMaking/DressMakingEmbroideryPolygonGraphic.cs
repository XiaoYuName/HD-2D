using UnityEngine;

namespace XFramework
{
    /// <summary>可序列化到 Prefab 的刺绣多边形绘制组件。</summary>
    [RequireComponent(typeof(RectTransform), typeof(CanvasRenderer))]
    public sealed class DressMakingEmbroideryPolygonGraphic
        : DressMakingEmbroideryPolygonGraphicBase
    {
    }
}
